export interface BasicQueryConditionObject {
    date?: string; // 选择的日期
    area?: string; // 地区
    credit?: boolean; // 是否使用贷记
}
export interface BasicQueryAreaObject {
    title: string;
    key: string;
}